on:
  push:
    branches:
      - master

name: Upload Release Asset

jobs:
  build:
    name: Upload Release Asset
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Publish Release
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: |
          yarn build-webapp && yarn deploy patch
